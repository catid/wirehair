# Installed WHV2 K8 lifecycle cost R0

Protocol `wirehair.wh2.k8-public-cost-r0`; sole namespace
`/var/tmp/wh2-k8-public-cost-r0`. The complete prospective freeze is in
`wirehair-sxvz.16.1.20.82.5.3.3`, before implementation or timing.
This measures the installed explicit WHV2 profile, not the prototype API.

## Workload and decision

Six actual API arms: ordinary independent WH2, explicit K8 independent, owned
WH1, ordinary borrowed WH2, explicit K8 borrowed, borrowed WH1. Ordinary K8
must select certified `0x4b295bbb47f4f9c9`; candidate descriptors must be
literal WHV2 / `0x7a9276b85c730ae0`. Candidate constructors use
`wirehair_v2_encoder_create_profile_id_with_options` for both policies,
matching installed recovery. Ordinary independent uses the no-options
constructor; ordinary borrowed uses its options constructor. All candidate
preparation, owned-basis allocation and copying stay inside timed creation.
No `wh2_small_*` implementation or prototype archive is linked.

Retain the prototype's six shapes: block bytes 2/64/1280, tail full/one byte,
message `7*B+tail`. Only ID7 emits tail bytes; every other packet emits B.
Encoder: create, descriptor, 24 encodes, free; IDs0..15 and
`UINT32_MAX-2*j`, j=0..7. Low/distant decoder streams each contain eight
repairs followed by systematics0..7. Charge create, all feeds through each
arm's own first success, recover and free. Maximum sixteen feeds; retain
systematic fallback. Do not equalize endpoints.

One common non-inlined WORK body, batch128, twelve replicates, both orders,
six shapes, three metrics, ten pairings, eighteen positions: 77,760 callbacks.
Retain two prelude observations and eight paired contrasts per panel, all 48
relative delay phases, every interrupted observation and complete attempted
API ledgers. All formatting/output occurs after measurement ends.

All 216 same-code 95% log confidence intervals must lie strictly inside
`+/-log(1.02)`. All 144 candidate upper95 log time-ratio bounds must be below
zero against both matched controls. There is no 5% minimum or candidate
regression tolerance. Failed controls mean CONTROL_FAIL; valid nonwinner FAIL;
infrastructure, identity or oracle failure INVALID. No rerun, filtering,
phase/order pooling, timing subtraction, resizing, retuning or subset rescue.

## Qualification and provenance

Reproduce all nineteen installed production objects and complete archives
under the original CMake flags before reusing those exact libraries. Freeze
source/header dependencies before compilation, compare actual post-compile
closure, pin all linker/runtime inputs before use, and verify one correctly
sized GF context. The original library qualification and all spent scientific
sources remain untouched; the new observer has its own protocol and build.

Independently derive the sealed lambda2 candidate coefficients. All six arms'
source-basis rows must reproduce their actual payload bytes and first-success
ranks. Check full/partial packet lengths, unused capacity, descriptors,
immutable borrowed inputs, independent source destruction, guarded repeated
neutral recovery and no live encoder before any fixture decoder.

Before science, qualify the complete roster and 216 real WORK cells per
backend, late-call error/exception cleanup, incomplete clocks, source and
tail-capacity corruption, installed descriptor rejection and deferred
publication with real output-device errors. Synthetic tests cover all strict
decisions, complete ledgers, binary CPU identity, launch receipts, caps,
timeout prefixes and spent namespaces. Native/portable/sanitizer fixtures
must agree. Repeated main and independent source reads precede the sole run.

Worker CPU240s/wall300s/WORK180s/address space384MiB; observer330s and whole
controller480s including verification. Raw192MiB, stderr64KiB, bundle256MiB.
Scientific execution is native only; portable and sanitizer workers reject
that mode. Native CPU50 retains the existing617-byte identity, SHA256
`3288e0ef61cf3e628dcd827f9cf003c9d6ec6b5a12169e7a8bfc796baacddba7`.
Use explicit subprocess environments, no allocator overrides, and
`ASAN_OPTIONS=detect_leaks=1:detect_stack_use_after_return=1:halt_on_error=1`,
`UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1`.

Commit/push qualified source before pinning and the single native launch.
Exact and independently written full retained audits precede result/source
advancement. PASS qualifies only these actual installed explicit static-call
lifecycles after process initialization on this host. It is not shared-call,
cold-start, non-GFNI-host, ordinary-default or all-K speed; not a new recovery
sample, proof of preserved-path restoration or current K3 speed retention.

## Independently audited installed result

The sole run at producing source `6f557dd7f548d6809973a8a0a2dd0cf512684198`
completed **PASS**. All 216 controls and all 144 strict comparisons pass;
the result is qualified for the actual installed explicit K8 API, not inherited
from the prototype. All source-policy, shape and order cells remain separate.

Across the six shapes and both orders, the independent-policy encoder is
2.153–2.879 times as fast as WH1 and the borrowed-policy encoder is
2.111–2.840 times as fast. Low-ID decoder ranges are 5.792–7.412 and
5.810–7.414 times as fast respectively; distant-ID decoder ranges are
2.955–4.429 and 2.956–4.429. These are ranges of individual point estimates,
not pooled estimates or confidence bounds. The weakest candidate comparison
against WH1 has an upper 95% time ratio of 0.477729. The same-code confidence
ratio envelope is 0.989362–1.008838, entirely within the frozen equivalence
bounds. Every candidate also passes against its ordinary-WH2 control.
The per-shape time-reduction table is in [the profile report](../V2_WIRE_PROFILE.md#installed-k8-full-lifecycle-speed-qualified).

All 77,760 callbacks, 4,320 panels, 34,560 paired contrasts and 17,280
phase-coverage cells pass the full independent chronology audit. Attempted
API counts (encoder create, encode, decoder create, feed, recover, free) are
`[3317760,79626240,6635520,53084160,6635520,9953280]`.
Every decoder reaches its own first success after eight packets. Timed WORK
is 38.577614860 seconds; whole-controller elapsed time is 149.622703180
seconds. All 160 involuntary switches and one minor fault within WORK remain
in the data; there are no major faults or voluntary switches there. No output,
interruption or clock cost is subtracted.

Before timing, exact and independent neutral qualification verified all three
nineteen-object production archives, complete compiler/link closures and
packet/rank fixtures. All 38 reader/build tests and all 16 independent-auditor
selftests passed under Python 3.12 and 3.8. Source review strengthened an
auditor-only framing test to require the intended rejection reason instead
of allowing a later hash mismatch to satisfy it. No scientific source or
decision rule changed. Repeated full source reviews are clean.

Both terminal exact replays pass with identical output and empty stderr.
The independent auditor imports only authenticated independent helpers, not
project controllers, and executes no codecs. Both interpreter audits reproduce
all statistical decisions exactly and verify the 915-pin launch receipt;
their maximum numerical difference from the controller is
`8.881784197001252e-16`. All audits and final pin checks finished before
producing HEAD or pinned documentation advanced.

Retained evidence:

- Bundle `/var/tmp/wh2-k8-public-cost-r0`, 135,206,221 bytes.
- COMPLETE SHA-256 `787021740f10034a3c7acba4ef033b990c4b3fe215f7692de240178f0953f942`.
- CLAIM / original receipt SHA-256 `413dc802331d68de4b4611cefe888ce53eacb26a208e92514ae1276350649020`.
- Raw SHA-256 `c28f29059652ccc3cfce7d3b29615b4e15224018c3411818ad478e7947fe65fa`.
- Analysis SHA-256 `07dee7ebba12086c9f0009bf37778eb7a0b564a882cb65d695b192eae68e7f6d`.
- Qualified builds, receipt and exact replays: `/tmp/wh2-k8-public-cost-qualified.8cEMid`.
- Independent audit: `/tmp/wh2-k8-public-cost-independent-audit.aipJc8Bp`;
  auditor SHA-256 `5fe68aefb57d09e4aed461d979ac90856b93fbe8f478ae2d9dadf57d4c51a5b0`.
- Independent terminal reports, Python 3.12 / 3.8 SHA-256:
  `2c955059df040ca5403d87562b590897f48eae67a0cfa2ad33764140406f85c5` /
  `73ee613144b380693fce1db1a5fe70a11186be5448661f515c998d216bdef498`.

This namespace is permanently spent. The result does not change defaults,
repair the existing preserved-path regressions, qualify current K3 speed
retention, or establish performance beyond the frozen installed-API scope.

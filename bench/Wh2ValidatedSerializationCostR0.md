# Validated-local serialization lifecycle cost R0

Prospectively frozen in `wirehair-sxvz.16.1.20.84.3.9` before new preparation
or candidate work. Protocol `wirehair.wh2.validated-serialization-cost-r0`;
sole scientific namespace `/var/tmp/wh2-validated-serialization-cost-r0`.
The sole run finished CONTROL_FAIL; the candidate is not retained.
Production and defaults are unchanged. The frozen contract follows, with the
independently audited result below.

Compare the exact installed K8 library with a nineteen-object relink replacing
only the already-qualified validated-local serialization Profile object.
This is not the rejected one-comparison validator. Reproduce every original
object, the archive and baseline DSO; recompile the fixed candidate exactly.

Retain the 20 preserved cases plus 18 actual ordinary K3/explicit K5/K8 cases:
three widths (2,64,1280 bytes), independent/borrowed policy, full messages.
The measured RunWork body is byte-identical to the prior qualified instrument.
Encoder work includes create, descriptor, K systematic plus eight low and six
distant packets, and free. Decoder includes create, feeds through its own first
success in the combined repair/systematic stream, recover and free.

Batch128 except K128 batch4; 12 replicates, both measurement orders, 18
positions, three pairs and all 48 conditioning phases: 98,496 callbacks per
separate DSO load order. Each load has 304 same-code and 152 treatment cells.
All same-code 95% log intervals must fit inside +/-log(1.02). Any treatment
lower95>0 is a regression; upper95>=log(1.02) is uncertain. Retain only a
global PASS plus one certified metric improving in all four load/measurement
order cells. No 5% minimum or resolved-regression tolerance. Retain every
observation; no filtering, pooling, resizing, retry, retuning or timing subtraction.

Worker caps: CPU210s, wall210s, WORK150s, address512MiB. Observer240s per load;
controller600s; raw192MiB per load, stderr64KiB, bundle448MiB. Format and emit
results only after measurement. Output failures remain terminal.

The new explicit hash-checked runtime derivation and counted C++ slot loops
authenticate six exact internal public bindings in current and five in candidate.
Only the internal serializer binding is removed; its public export remains.
Preserve all 53 public exports, 37 shared runtime targets, separate GF contexts,
all metadata/receipt recomputation, oracles, WORK and error checks. Historical
files and spent receipt verifiers are never edited or invoked as new campaigns.

Qualify native and ASan/UBSan observer drivers, including the complete neutral
roster, routing/packet/rank fixtures, late failures, exceptions, partial clocks,
source corruption, broken-pipe and full-output errors. The DSOs themselves are
native; prior 28-test candidate qualification separately covers instrumented
codec code. Commit and pin the complete instrument before the sole launch.
Complete exact and independent raw/statistical/provenance audits before
advancing producing HEAD or pinned reports.

This screen concerns current-to-candidate cost and retention, not recovery,
WH1 superiority, pre-admission restoration, default admission, cold-start,
shared-vs-static generalization, non-GFNI or all-K qualification. The serializer
does not run in the decoder and cannot directly explain decoder/unchanged-WH1
regressions. Broader structural/default/all-K work remains required.

## Independently audited terminal result

The sole run at producing source
`7c4feb92f3538f7a37635d514931de6668f2b639` completed normally. Both workers
exited zero with empty stderr, without codec, oracle or infrastructure errors.
All 196,992 records were retained; no rerun, filtering or decision change was
used. The scientific namespace is permanently spent.

| DSO load order | Failed same-code controls | Slowdown cells | Uncertain cells | Outcome |
| --- | ---: | ---: | ---: | --- |
| Current then candidate | 2/304 | 59/152 nominal | 54/152 | CONTROL_FAIL |
| Candidate then current | 0/304 | 66/152 resolved | 49/152 | REGRESSION |

Slowdown and uncertainty sets can overlap. There are zero certified metrics
improving in all four load/measurement-order cells; `candidate_retained=false`.
The reverse load's clean controls independently expose regressions, even
though the complete screen's terminal outcome is CONTROL_FAIL.

Both failed controls compare candidate decoder with itself. Opt-in K3/B2,
independent input, measurement order 1 has time ratio 0.9952022687 with 95%
interval [0.9776300328, 1.0130903537]. Ordinary K3/B64, borrowed input, order 0
has ratio 0.9955323052 and interval [0.9773507285, 1.0140521123]. Both include
equality: they fail equivalence precision, not a resolved directional bias.
In the independently controlled reverse load, certified K2/3/4/6 encoder
estimates are 0.566–4.477% slower and decoder estimates 1.786–4.253% slower;
all twenty encoder and twenty decoder comparisons are resolved.

This is not proof that removed serialization work causes decoder or WH1
slowdowns. Removing the internal serializer binding also moves unchanged code
addresses; no unique causal attribution is established.

Observer times were 163.831860094 and 164.725968355 seconds; timed WORK was
39.767390760 and 39.878933023 seconds. The full controller took 339.268513441
seconds. These are whole-experiment totals, not codec latencies.

Exact retained replay passed under Python 3.12 and 3.8. A separately written
standard-library auditor also passed on both, producing byte-identical
reports before HEAD or pinned documentation advanced. It checked all 1,081
receipt pins (4,144 pin checks), complete packet arithmetic, prefix ranks,
record chronology, API ledgers, phase coverage and statistical decisions.
Maximum arithmetic difference was below `1.11e-15`; all thirteen auditor
selftests pass under both interpreters. Repeated source-reading passes found
no remaining bug. The prior native/ASan observer qualification covers the
drivers; the loaded DSOs are native, with instrumented codec correctness
covered separately by the earlier 28-test qualification.

Retained evidence:

- Bundle `/var/tmp/wh2-validated-serialization-cost-r0`; COMPLETE SHA256
  `3273d539e6f60e045c17229f1dd48311efe02da845b17c32743db8f8c1125406`.
- Analysis SHA256
  `bc8c7ac2ec28b6cf90dc877b4896134ba3f7bd7c285e1a61cea5f8c13533e9fc`.
- Current/candidate raw SHA256
  `2d7a45a5f28deecac9d71f3f53d9cf734123995a487eefda23dd7dadc8056cb5`;
  reverse raw SHA256
  `14c662cafc490895236669c0b0f201e13bbd4da7bc5b3b9eec61bdc02d5db8fd`.
- Qualification and receipt:
  `/tmp/wh2-validated-serialization-cost-qualified.hiViJURA`, receipt SHA256
  `13e0bcdd3167f53df2bb10c991ebac19b799e3e5f7fccc8a3fc83d37045892f3`.
- Independent audit:
  `/tmp/wh2-validated-serialization-independent-audit.6jnhlX4S`, auditor SHA256
  `592d792aa3950a57c6a033f344fd8969fe37511d3376c25650d5c529056ca24f`;
  both full report SHA256
  `5a4fd4ad717c5e717f18217baa25f30a5494736c14092c488c6c11404872fb33`.

No production promotion follows. Existing K8 explicit-profile wins remain
separate evidence; preserved-path restoration, current K3 retention, K5
installed speed and the full all-K objective remain unmet. Do not invoke this
run's strict current-HEAD verifier after documentation/source advancement.

# K8 retained paired recovery R0

Protocol `wirehair.wh2.k8-serialized-recovery-r0`; sole scientific namespace
`/var/tmp/wh2-k8-serialized-recovery-r0`. The prospective freeze is in
`wirehair-sxvz.16.1.20.82.5.2.1`, before implementation or candidate workloads.
No timing, new roots, equation selection, installed profile or promotion.

## Retained comparison

Each of native, portable arithmetic and ASan/UBSan replays exactly 6,260
records: 6,144 retained-fresh traces, 72 hard traces and all 44 historical
origins, in their original order, widths, tails and horizons. There are
74,946 packet IDs. The fresh roster shares 512 roots across three widths
(2/64/1280 bytes) and four loss schedules. Backends and policies are semantic
replays, not additional independent samples. The exact sealed K8 lambda-2
equations and fixture source are unchanged.

The six arms are ordinary WH2 independent, K8 independent, WH1 owned,
ordinary WH2 borrowed, K8 borrowed and WH1 borrowed. Ordinary constructors
must actually select the certified WHV2 descriptor; they cannot route to the
candidate. Candidate identity is WHK8/v1/32, `0x5748324b38544d31`.

Message size is `7*B+tail`. Only ID7 emits `tail` bytes; all other packets
emit B. Oracle arithmetic zero-pads the last source block. Hashes and feeds
use meaningful bytes; every untouched capacity byte stays guarded. Each arm
creates a real encoder and eight source-basis probes, checking the actual
returned coefficients and payloads. Independent sources are poisoned/freed;
borrowed sources are checked for mutation. Every encoder across all six arms
is destroyed before any receiver. Each receiver stops at its own first success,
which must equal independent source-equation rank, then recovers twice into
guarded output. Per-arm attempted-call ledger is
`[9,9*count,1,first-or-count,2-if-success-else-0,10]`.

## Frozen decisions

Three distinct predicates are reported:

1. `execution_valid`: exact roster/provenance, every-arm coefficients,
   packets, guards, ranks/statuses, ledgers and complete backend agreement.
2. `retained_target_qualified`: both candidate policies have at most 1% OH0
   failures overall and in every 512-record cell (at most five), recover all
   72 hard cases at OH0 and all 44 histories within original horizons.
3. `retained_paired_oh0_superior`: fixes exceed introductions on retained-fresh
   OH0 for each pair `(1,0), (1,2), (4,3), (4,5)`.

PASS requires all three. A valid nonwinning result is FAIL; infrastructure,
provenance, incomplete output or oracle failure is INVALID. Report fixed and
introduced identities at OH0..4 for all groups and fresh cells; omit historical
horizons unavailable in the original sequence. Net superiority does not mean
zero introductions, every-cell dominance or population statistical significance.

## Neutral qualification and limits

The 48-case neutral roster is four fixed twelve-ID streams (0..11, 8..19,
`UINT32_MAX-2*j`, and all-zero IDs), three widths and full/tail1 shapes,
followed by the first twelve and last twelve retained-fresh records. These
replays check instrumentation; they cannot select or retune the candidate.

Twelve late-fault checks cover full and partial B2 shapes: the final 108th
encode's length, exception and unused-capacity mutation; eighth-feed error;
second recovery's length and guard. Require exactly nine or ten created/freed
candidate handles as appropriate, with the fault actually reached. The final
fault-encode ID is7 to exercise un-emitted tail capacity.

Worker limits: 180 CPU seconds, 210 wall seconds, 384 MiB address space except
ASan shadow. Observer limit 245 seconds/backend; whole controller 900 seconds
including verification; raw96 MiB/backend, stderr64 KiB, sealed bundle384 MiB.
Every result is retained; no filtering, resizing, rerunning, retuning or rescue.

Reuse qualified K8 boundary archives and matching production archives, not
new production builds. Preserve closed historical producing dependencies and
original SmallCore/README bytes explicitly; do not rerun stale old verifiers
or substitute current documents for pinned history. New worker dependencies,
compiler/linker/runtime and fixture generation receive their own provenance.
ASan must match the archive's native GF context ABI. Repeated source reviews,
neutral/controller tests, committed/pushed source precede the sole launch;
exact and independent full arithmetic/raw/provenance audits precede reporting.

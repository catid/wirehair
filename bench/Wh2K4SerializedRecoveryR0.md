# K4 retained paired recovery R0

Protocol `wirehair.wh2.k4-serialized-recovery-r0`; sole scientific namespace
`/var/tmp/wh2-k4-serialized-recovery-r0`. Prospectively frozen in
`wirehair-sxvz.16.1.20.82.6.1.1.1` before observer implementation or new
candidate workloads. No timing, new roots, equation selection, installed
profile or default promotion.

## Retained comparison

Replay exactly 6,254 records on each of native, portable arithmetic and full
ASan/UBSan: 6,144 retained loss traces, 72 hard traces and all 38 historical
origins. Preserve original order, widths, tails and horizons. The 6,216
loss/hard records contain eight IDs each; the histories contain 163 IDs,
giving 49,891 packet IDs overall. The 37 unique historical prefixes are not
a replacement for their 38 origins. The 31 retained boundary windows and
2,252 coefficient rows remain fixture provenance, not additional recovery
records. Backends and source policies do not add independent samples.

The six arms are ordinary WH2 independent, K4 independent, WH1 owned,
ordinary WH2 borrowed, K4 borrowed and WH1 borrowed. Ordinary WH2 must
actually return the certified WHV2 descriptor, never the candidate profile.
Candidate identity is `WHK4`/version 1/size 32, `0x5748324b34544d31`.
Use the exact sealed GF(256) lambda-1 pair `(64,120,54,15)` /
`(65,120,54,15)` and 20,480-byte lookup. The source structural bundle is
`/var/tmp/wh2-k4-thue-morse-r0`, COMPLETE SHA256
`8f1c9357b125edffc282df4cde29592130e27566f86946a7a85b0e92f175d281`.

Message length is `3*B+tail`; only ID3 emits `tail` bytes. All other packets
emit B bytes, with the last source block zero-padded in the independent
oracle. Hash and feed only meaningful bytes; guard every unused capacity byte.
Each arm creates one real-message encoder and four source-basis encoders.
Poison/free independent inputs and check borrowed inputs for mutation.
Destroy all encoders in all six arms before any receiver exists.

Each decoder stops at its own first success, which must equal independent
source-equation rank, then recovers twice into guarded output. Per-arm
attempted-call ledger is
`[5,5*count,1,first-or-count,2-if-success-else-0,6]` for encoder create,
encode, decoder create, feed, recover and free. Observe real control rows via
their basis encoders; independently derive K4 rows from its sealed matrices
and verify every arm's payload with polynomial arithmetic.

## Frozen decisions

Report three separate predicates:

1. `execution_valid`: exact roster/provenance, every arm's coefficients,
   packets, guards, ranks/statuses, ledgers and complete backend agreement.
2. `retained_target_qualified`: both candidate policies have at most 1% OH0
   failures overall and in every 512-record width/schedule cell (at most
   five), recover all 72 hard cases at OH0 and all 38 histories within their
   original horizons.
3. `retained_paired_oh0_superior`: retained-loss OH0 fixes exceed introductions
   for each pair `(1,0)`, `(1,2)`, `(4,3)`, `(4,5)`.

PASS requires all three. A valid nonwinner is FAIL; infrastructure, incomplete
output, provenance or oracle failure is INVALID. Report OH0..4 failure counts
and fixed/introduced identities for every group and loss cell. Do not invent
historical overhead horizons absent from the original sequence. Aggregate
superiority is not zero introductions, every-cell dominance, population
statistical significance or a universal recovery guarantee.

## Neutral observer qualification

The 48-case neutral roster uses four eight-ID streams (0..7, 4..11,
`UINT32_MAX-2*j`, all-zero IDs), three widths (2/64/1280) and full/tail1
shapes, followed by the first twelve and last twelve retained-loss records.
These checks cannot select or retune equations or change the retained roster.

Twelve late-fault checks cover full and partial B2 shapes: the final 40th
encode's length, exception and unused-capacity mutation; fourth-feed error;
second recovery's length and guard. The injected error must be reached, with
exactly five created/freed candidate handles for encode errors and six for
feed/recovery errors. Final fault-encode ID3 exercises un-emitted tail space.

The shared worker's only dimension change is its compile-time window
provenance assertion: K4 has 31, whereas its existing K8 user keeps 30.
The immutable K8 wrapper and scientific protocols are not rewritten.
Repeat bounded K8 neutral checks in fresh builds when changing shared code;
never rerun its spent scientific workload or old source-sensitive auditors.

## Launch prerequisites and limits

Worker limits: 180 CPU seconds, 210 wall seconds, 384 MiB address space except
ASan shadow. Observer limit 245 seconds/backend; controller 900 seconds total;
raw 96 MiB/backend, stderr 64 KiB, sealed bundle 384 MiB. Retain every result;
no filtering, resizing, rerunning, retuning, new roots or subset rescue.

Reuse the qualified K4 serialized archives at
`/tmp/wh2-k4-serialized.yegpP9w9/{mode}/k4` and their matching production
archives at `/tmp/wh2-v2-k8-admission.YNN2XmAx/{mode}`. The latter contain
19 members, not the 18 assumed by the old K8 prototype builder. Boundary
records use the 105-test FROZEN/RESULT schema and different ASan compile flags.
Archive identity alone is not complete scientific producing provenance.

Before science, qualify a new controller/reader/build receipt with exact
original-source, object, compiler/linker/runtime and dependency closure.
Preserve changed source/documentation bytes through explicit authenticated
historical snapshots, never by rebinding old receipt paths to current files.
Match the private GF context ABI and sanitizer settings of each linked archive.
Require independent payload/rank/ledger and parser/decision mutation tests,
bounded launch/output/failure tests, repeated main and independent source
review, and committed/pushed qualified sources before the sole launch.
Complete exact and independent full raw/provenance audits before advancing
pinned source or reporting a verdict.

The initial C++ observer and its neutral checks are only preparation.
No scientific comparison or timing is launched by adding these files.
Full encoder AND decoder lifecycle speed is separately tracked in
`wirehair-sxvz.16.1.20.82.6.1.1.2`. Installed/public/default admission,
preserved-path regression repair and the all-K objective remain outstanding.

## Initial C++ observer preparation result

The K4 wrapper and compile-time window assertion pass bounded neutral checks
in fresh native, portable and full ASan/UBSan builds. The existing K8 wrapper
also passes in each backend without edits. Each of these six observer builds
checks the exact contract, all 48 neutral records, twelve late-fault cleanup
cases, and unknown-mode rejection. Neither scientific worker mode nor a new
recovery/timing campaign is invoked; the K4 scientific namespace is absent.

Artifacts and the neutral-only runner are retained at
`/tmp/wh2-k4-recovery-neutral.QIk1VHi5`. The native/scalar/ASan `RESULT.json`
digests are respectively:

- `47e199a8e9cb00fad2af22aa156c72551308eaddc4ffbf020769ed471ab96fe8`
- `4ad919985715f6c51ae7de531eeef433f30d46b96ab12264d649c1bef00016e4`
- `6938d321a10c60f064fcf3362dd98eaed4c20fdcb0381a5f86e0e4d1ac264d9b`

Each backend retains all twenty build/inspection/test commands. The runner
authenticates the completed serialized audit, its backend RESULT/FROZEN
records, actual boundary archives/fixtures and matching production archive.
Current compiler dependencies are checked before and after compilation;
linking is non-LTO. Actual private GF contexts have 141,328/137,232/157,728
bytes respectively, and the production archive has nineteen members.
These checks do not close the full original tool/runtime/producing chain
required for a scientific receipt.

An independent read-only audit checks all 288 neutral records, 1,728 arms and
17,280 real-message packet hashes across K4/K8 and the three backends. It
rebuilds GF(256) multiplication, candidate coefficient matrices, every arm's
payload hashes, incremental rank/first-success, feed/recovery results and
complete ledgers. Exact neutral rosters, partial tails, ownership/backend
agreement and invocation scope pass. All 514 distinct artifact/input identities
are rehashed before and after the audit. Repeated source-reading passes find
no confirmed bug; no codec or scientific worker is run by the auditor.

The durable script and report are at `/tmp/wh2-k4-neutral-independent.MwOgm4fa`.
Python 3.12.3 is the only interpreter claimed for this neutral audit.
The 16,800-byte `audit.py` SHA256 is
`3e9719f162e2a846537ece650a1cdd7354f17454d9e1c470a4b1f8bd6a9cd969`;
the 4,465-byte `report-python312.json` SHA256 is
`c0eb933a3e4493aa2eb3d179a8ee031e0cf45cb825b9f619bda59df13edde128`.
The archival audit passes on its first attempt, with no diagnostic exits.
The runner's initially pinned contract bytes are preserved in
`Wh2K4SerializedRecoveryR0-pre-result.md` in the preparation directory,
SHA256 `a24867920c156c206d2f8ba9161dcaac30bf30f80bef7eac944bdd368a422ea4`.
Do not rerun source-sensitive archived audits after this documentation advances.

This completes only initial observer preparation. The new controller, reader,
decision/failure mutation tests and complete producing receipt still precede
the sole full retained comparison. No paired superiority, new recovery rate,
speed, installed profile or default-promotion result follows.

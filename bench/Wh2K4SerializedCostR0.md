# K4 serialized lifecycle cost R0

Protocol `wirehair.wh2.k4-serialized-cost-r0`; sole scientific namespace
`/var/tmp/wh2-k4-serialized-cost-r0`. Prospectively frozen in
`wirehair-sxvz.16.1.20.82.6.1.1.2` before new speed code or timing workloads.
This measures the qualified WHK4 benchmark boundary, not an installed profile.
The separately audited retained recovery PASS is not speed evidence.

## Frozen workload and decisions

Preserve GF(256), the sealed lambda-1 companion pair `(64,120,54,15)` /
`(65,120,54,15)`, its 20,480-byte lookup and WHK4 descriptor
`0x5748324b34544d31`. Compare ordinary independent WH2, independent K4,
owned WH1, borrowed WH2, borrowed K4 and borrowed WH1. Both ordinary WH2
routes must still select the actual certified WHV2 descriptor.

Six shapes use block bytes 2/64/1280, each with full or one-byte tail.
Message length is `3*B+tail`; only ID3 emits tail bytes. Every other packet
emits B bytes. Encoder work includes create, input preparation, descriptor
output, all twenty encodes and free: IDs0..11, then `UINT32_MAX-2*j`, j0..7.
The low-ID decoder receives repairs4..11 then systematics0..3; the distant-ID
decoder receives the eight distant repairs then systematics0..3. Charge create,
every feed through each arm's own first success, recover and free, with at most
twelve feeds. Do not equalize endpoints. Systematic fallback means more than
eight feeds, not more than four.

The existing common `Wh2SmallLifecycleWorkerR0.h` is unchanged. A six-line
static wrapper selects K4, eight repairs, the new protocol and new claim path.
One non-inlined WORK body includes the C wrapper overhead. Output storage is
preallocated equally outside WORK for every arm; all result formatting/output
occurs after measurement. Unemitted partial-packet capacity retains its 0xa5
guard in the full output comparison, independently of meaningful payload bytes.

Batch128, twelve replicates, both orders, six shapes, three metrics, ten
pairings and eighteen positions give 77,760 callbacks. Retain two preludes
and eight paired contrasts per panel, all 48 relative phases, all interrupted
observations and complete attempted-call ledgers. All 216 same-code 95% log
confidence intervals must lie strictly inside `+/-log(1.02)`. Every one of
144 candidate upper95 log time-ratio bounds must be below zero, separately
against matched actual WH1 and certified WH2. There is no 5% minimum.

Failed controls mean CONTROL_FAIL; a valid nonwinner means FAIL; incomplete
execution, identity, provenance, oracle or infrastructure failures mean INVALID.
Never rerun, filter, subtract pauses, pool phases/orders, resize, retune or
rescue a subset after seeing results. Every launched namespace is spent.

## Qualification and provenance

Reuse the unchanged K4 boundary archives at
`/tmp/wh2-k4-serialized.yegpP9w9/{mode}/k4` and matching nineteen-member
production archives at `/tmp/wh2-v2-k8-admission.YNN2XmAx/{mode}`. The new
builder uses the qualified archive reader as a read-only producing-data parser,
never invokes historical builders/controllers/verifiers, and reproduces only
the K4 boundary object/archive in a fresh external directory. Authenticate
original producing commands, objects, dependency closures and historical source
snapshots, not just archive digests. Preserve old source pins as old bytes;
do not rebind changed documentation or the historical SmallCore header.

Freeze inputs before compilation/linking and verify actual compiler/linker
closure afterward. Match private GF context sizes (141328/137232/157728 bytes
for native/scalar/ASan) and sanitizer/ISA flags. The new receipt must bind the
actual executable, fixture and native target artifacts explicitly, plus all
qualified backend manifests and inputs. Require identical backend fixtures.
Sanitizers enable leak and fake-stack checks; seven allocator/loader overrides
are absent. Child processes use an explicit environment.

Independently derive candidate rows from polynomial arithmetic and its sealed
pair. Every arm's observed source-basis rows must reproduce all meaningful
payload bytes, padding guards and first-success ranks. Check independent-source
destruction, borrowed-source immutability, all encoders destroyed before fixture
receivers, exact descriptors, capacities and repeated guarded recovery.

Before science, qualify all 216 actual WORK cells, the full roster, late
encode/feed/recovery error and exception cleanup, partial clocks, source and
tail-capacity corruption, all five deferred-publication cases, mock writer
failures, `/dev/full` and broken pipes. Positive/negative claim checks must not
create a scientific namespace. Reader/build synthetic tests cover decisions,
omissions, receipt/fixture substitution, environment/deadline handling and
output-cap/timeout prefix retention under Python 3.12 and 3.8. Repeated main
and independent source-reading reviews must pass before launch.

Worker CPU240s/wall300s/WORK180s/address space384MiB; observer330s and whole
controller480s including verification. Raw192MiB, stderr64KiB, bundle256MiB.
Only native may run scientific timing; scalar/ASan workers reject it. Native
CPU50 retains its existing 617-byte binary identity, SHA256
`3288e0ef61cf3e628dcd827f9cf003c9d6ec6b5a12169e7a8bfc796baacddba7`.

Commit/push qualified source before receipt and the sole launch. Exact replay
and a separately implemented full raw, arithmetic, chronology, ledger,
statistical and provenance audit must complete before pinned source/docs or
HEAD advance. PASS would qualify only these benchmark static-call lifecycles
after initialization on this host: not installed/default, shared-call,
cold-start, non-GFNI-host or all-K speed, a new recovery sample, restored
preserved paths or zero bad construction seeds throughout all K.

## Preparation status and lost historical build inputs

The first draft's synthetic tests pass (45 tests per Python 3.12/3.8).
Independent source review found and prompted fixes for expired preflight
consuming a namespace, insufficient INVALID-result replay checks, and lost
output-device failure diagnostics. These are benchmark-controller fixes;
the shared C++ WORK body and production codec are unchanged.

On 2026-09-14, all three neutral-build preflights stopped at missing historical
`/tmp/wh2-k8-public-recovery-qualified.i7v7bPnq/{mode}/manifest.json` files,
before creating their build outputs or compiling. The referenced production,
K4 boundary and independent audit directories were also absent. No scientific
namespace was created. The disappearance's cause was not established.

The old build-reuse route above therefore remains unavailable and must not be
silently redirected to new files. `Wh2K4FreshNeutralR0.py` starts a distinct
current-source engineering lineage in durable task-owned storage, preserving
all attempts. It builds current libraries and runs their selected correctness
tests plus the seven direct/serialized retained K4 tests. Its initial result
explicitly does **not** qualify complete producing/tool/runtime closure or
timing. Independent artifact review and a new explicitly bound provenance
reader/receipt remain necessary before the frozen speed experiment can launch.
Equations, fixture bytes, workload, decision rules and the unspent scientific
namespace are unchanged; historical recovery is not rerun or reinterpreted.

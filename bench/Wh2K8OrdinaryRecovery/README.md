# Ordinary K8 retained recovery R0

Prospectively frozen in `wirehair-sxvz.16.1.20.82.5.3.4.2`. Protocol
`wirehair.wh2.k8-ordinary-recovery-r0`; sole future scientific namespace
`/var/tmp/wh2-k8-ordinary-recovery-r0`. The sole run completed independently
audited PASS. Its namespace is permanently spent; defaults remain unchanged.

Derive a fresh external observer, controller, builder and synthetic tests from
five complete SHA-pinned historical K8 public-recovery templates. They are
inert text, never imported, patched or executed. New output hashes and the
generator are bound by `DERIVATION.json`. Preparation exposes only prepare,
verify and build; no run/receipt/replay forwarding.

| Arms | Actual constructor route |
| --- | --- |
| 0 / 3 | Explicit certified WH2, independent bare / borrowed options |
| 1 / 4 | Candidate ordinary WH2, independent bare / borrowed options |
| 2 / 5 | Unchanged actual WH1 owned / borrowed |

The candidate is the same six-line selector qualified and timed previously.
Source SHA256 `172324fd048daf154267f60a6e76df1e1b9a2545b9eac49059d0be21cfe99a3f`.
Reuse the exact candidate source/object/recipe validation from
`Wh2K8OrdinaryCost/Candidate.py`, not its old derivation verifier or controller.
The new builder verifies this recovery derivation directly. All nineteen
original archive objects and the complete archive reproduce byte-exactly
before the candidate is linked as an explicit object ahead of the archive.
The original Profile archive member must not be extracted; one actual WHV2,
WH1 and matching GF runtime remain. Candidate source, qualified/reproduced
objects, compiler dependencies, recipe and forty-test selector log are pinned.

Preserve exactly 6,260 records and 74,946 packet IDs: 6,144 retained loss
traces, 72 hard cases and 44 historical origins. Preserve widths, tails,
packet order and unextended horizons. No new roots, seeds, windows, losses,
extra samples or treatment workload. Preserve own-first-success feeding,
independent-source destruction, borrowed-input immutability, all senders
destroyed before receivers, polynomial packet/rank oracles, guarded repeated
recoveries and complete attempted API ledgers.

Require at most 1% OH0 failure overall and in every 512-trace cell (at most
five failures per cell), every hard case at OH0, every history within its
original horizon, and fixes greater than introductions against both matched
controls. Report OH0 through OH4 and all introductions. A valid nonwinner is
FAIL; oracle/provenance/backend/infrastructure failures are INVALID. Preserve
all failed evidence; no rerun, filtering, retuning or subset rescue.

Add exact complete-record parity against pinned installed K8 native recovery
data, including candidate WHV2 descriptors. The existing prototype parity
remains separate and permits only its validated WHK8/WHV2 descriptor change.
All three installed neutral fixtures also agree exactly. New historical inputs
are pinned in every build and required by the current receipt verifier.

Native, portable and fully instrumented ASan/UBSan builds retain 48 neutral
cases each, twelve late-call cleanup checks, descriptor rejection, all-arm
field/rank/ledger mutations, repeated-equation detection, threshold/nonwinner
tests, pin omission/backend rebinding, caps, partial publication and spent
namespace checks. ASan leak and fake-stack checks remain enabled.

Per-worker limits: CPU180s, wall210s, address space384MiB (ASan shadow exempt),
observer245s; all-backend controller900s, raw96MiB/backend, stderr64KiB/backend,
bundle384MiB. No concurrent lifecycle timing. Commit/push qualified sources
before the sole retained comparison. Complete exact replays and an independent
full arithmetic/raw/build audit before advancing HEAD or pinned documents.

This qualifies retained recovery through actual ordinary constructors, not a
new independent recovery sample or timing. Pre-admission preserved-path
restoration, current K3 speed retention, broader hardware/K coverage and the
full speed/recovery objective remain separate from this gate and default
promotion. Historical scientific namespaces stay spent and untouched.

## Independently audited ordinary-route result

The sole retained comparison at producing source
`adeb96cb92e3930940710913b18d3db7de4ce588` completed **PASS** on 2026-09-10.
These are actual ordinary independent-bare and borrowed-options constructors,
not explicit-profile calls substituted for the candidate. Both policies give
the same complete records. Failures on the 6,144 retained loss traces:

| Codec | OH0 | OH1 | OH2 | OH3 | OH4 | Observed OH0 rate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Ordinary K8 candidate | 12 | 0 | 0 | 0 | 0 | 0.1953125% |
| WH1 | 37 | 5 | 3 | 2 | 0 | 0.6022% |
| Explicit certified WH2 | 246 | 3 | 0 | 0 | 0 | 4.0039% |

OH0 means exactly eight packets; OH1 through OH4 allow one through four extra
packets. Ordinary K8 fixes 37 WH1 failures and introduces twelve different
failures. Against certified WH2 it fixes 245 and introduces eleven, with one
shared failure. That is 67.6% and 95.1% fewer aggregate OH0 failures,
respectively, not per-trace dominance or a universal guarantee. Every candidate
failure recovers with one additional packet. Every 512-trace width/schedule
cell meets the frozen 1% limit; the worst has 3/512 failures (0.5859375%).

All 72 separate hard cases recover at OH0 for K8 and WH1. Certified WH2 has
six failures at OH0, one at OH1 and none at OH2. All 44 historical origins
recover for K8 at their original widths/tails and unextended horizons, versus
five unresolved WH1 and 41 unresolved certified-WH2 origins. These hard/history
cases are not in the loss denominator. All twelve loss cells share the same
512 roots; policy/backend replays are not additional independent samples.

All 6,260 records and 74,946 packet IDs are retained. Each backend's declared
six-operation ledger (encoder create, encode, decoder create, feed, recover,
free) is `[338040,4047084,37560,301114,74936,375600]`: 375,600 fresh handles,
449,676 real-message packets plus 3,597,408 basis packets, and 74,936 guarded
successful recoveries. Every sender is destroyed before receivers are created.
The controller completes in 67.277342444 seconds with all workers successful
and empty stderr; this elapsed duration is not codec performance evidence.

Native, portable and full ASan/UBSan agree on all complete six-arm records,
including every descriptor, packet, row, feed status, first success and ledger.
Direct installed parity permits no descriptor exception; the separate inherited
prototype comparison permits only its independently validated descriptor change.
All nineteen original objects and archives reproduce byte-for-byte, as do all
three previously qualified selector objects. Every link map extracts exactly
sixteen original archive members, excludes the old Profile and uses one matching
GF runtime. The 57 recorded build/qualification commands per backend all have
their required exit statuses; the 48 neutral cases and twelve late-call cleanup
checks pass per backend.

Root derivation/parity tests pass 8/8, generated controller/build tests 27/27,
and independent auditor tests 23/23, each under Python 3.12 and 3.8. Both exact
retained replays and both independent full arithmetic/raw/build audits pass
before HEAD or pinned documents advance. The independent auditor imports no
project controllers and executes no codecs. It verifies all 1,346 unique receipt
pins, every generated-source delta, all command/recipe/object/link closures,
installed/prototype parity and every summary cell and fix/introduction list.
Complete summaries agree exactly across interpreters. Repeated main and
independent source reviews found no remaining defects.

Retained evidence:

- Bundle: `/var/tmp/wh2-k8-ordinary-recovery-r0`, 136,331,314 bytes.
- COMPLETE SHA256: `22c461bb67fe7458d6aa36e5272fe96ec3deeb7f3cf8228485305336ae4ac992`.
- Native raw SHA256: `2a6a8abda8e7f25bbaa3063041900e598ad3179589c61a4a4b587c3bdcc942ce`.
- Analysis SHA256: `ebcc1d412455a82222eb7313e06610a061181247b22f27f74f744b8c07802f08`.
- CLAIM/original receipt SHA256: `d380578e75176ae890e3fb605d8cdcd2624d11c488859dae5b37045b7d958d36`.
- Builds, receipt and exact test/replay/terminal logs: `/tmp/wh2-k8-ordinary-recovery-prep.1oQfwbsg`.
- Independent scripts, tests, prelaunch and terminal reports: `/tmp/wh2-k8-ordinary-recovery-independent-audit.NbDxG7Oy`.
- Frozen auditor SHA256: `a8faf781293f30eb91cfe2c38fdd56f64d1ff09f56c46a97dcbede691a86b73f`.
- Python 3.12 terminal report SHA256: `4378046da498f51a90706ca8754b68e775847fe53a880ae5a05d3a44b4612558`.
- Python 3.8 terminal report SHA256: `f854d1c204a334dc9d6f46668e93334372dd7043bf0ff99acafe70b64c962ad1`.

Ordinary K8 now has separate qualified static speed and retained recovery gates.
Production still selects certified WH2 at K8. Pre-admission preserved-path
restoration and current K3 speed retention remain required; preparation is
tracked in `wirehair-sxvz.16.1.20.82.5.3.4.3`. No default, all-K, new-sample or
zero-bad-construction-seed claim follows. Never rerun this namespace or invoke
its strict current-HEAD replay after this report advances; use the retained
completed audits instead.

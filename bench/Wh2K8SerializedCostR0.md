# K8 serialized lifecycle cost R0

Protocol `wirehair.wh2.k8-serialized-cost-r0`; sole namespace
`/var/tmp/wh2-k8-serialized-cost-r0`. The complete prospective freeze is in
`wirehair-sxvz.16.1.20.82.5.2.2`, recorded before source or timing workloads.
This is a new benchmark-boundary speed gate, not installed/default admission.

## Frozen workload and decision

Reuse the sealed lambda-2 K8 GF(256) equations and unchanged qualified
native/portable/ASan boundary and production archives. Six actual C API arms:
ordinary independent WH2, independent K8, owned WH1, borrowed WH2, borrowed
K8, borrowed WH1. Ordinary K8 must actually select the certified descriptor.

Six shapes use B=2/64/1280 and tail=B/1. Message size is `7*B+tail`; only
systematic ID7 emits tail bytes. Every other packet emits B bytes. Encoder
work includes create, descriptor output, 24 encodes and free: IDs0..15 and
`UINT32_MAX-2*j`, j=0..7. Low/distant decoder streams contain their eight
repairs followed by systematics0..7. Charge create, every feed through each
arm's own first success, recover and free. Maximum16 feeds; record systematic
fallback when first success exceeds eight. Do not equalize endpoints.

The static wrapper selects a new common future worker; spent K2/K5 worker
sources are unchanged. One common non-inlined WORK body includes caller C
wrapper overhead. Output storage is preallocated equally outside WORK for
every arm. All result formatting/publication follows measurement completion.

Batch128, 12 replicates, two orders, six shapes, three metrics, ten pairings,
18 positions: 77,760 callbacks. Retain two preludes and eight paired contrasts
per panel, all 48 relative delay phases, and every interrupted observation.
All 216 same-code 95% log confidence intervals must fit strictly inside
`+/-log(1.02)`. All 144 candidate upper95 log time-ratio bounds must be below
zero against both matched controls. There is no 5% minimum improvement.

Failed controls mean CONTROL_FAIL; valid nonwinning candidate comparisons
mean FAIL; infrastructure/identity/oracle failures mean INVALID. No rerun,
filtering, phase/order pooling, pause subtraction, resizing, retuning or rescue.

## Qualification and limits

Every arm's actual source-basis rows must reproduce its real payload under
independent polynomial arithmetic and its first success under normalized
Gaussian rank. Derive candidate rows independently from the fixed companion
pair. Check source independence, actual borrowed-input immutability, tail and
unused packet capacity, descriptors, feed status, guarded repeated neutral
recovery and exact attempted-call ledgers. All encoders are destroyed before
any fixture receiver is created.

Before science, qualify the full roster, 216 actual WORK cells, native/scalar/
ASan fixture agreement, last-call error/exception cleanup, partial clocks,
source corruption, partial-tail capacity mutation and deferred publication/
real output-device failures. Synthetic tests exercise every candidate decision,
failed controls, target/receipt rejection, timeout/cap prefixes and spent paths.

Worker CPU240s/wall300s/WORK180s/address space384MiB; observer330s and entire
controller480s including verification. Raw192MiB, stderr64KiB, bundle256MiB.
ASan shadow is exempt from the address-space cap; scalar/ASan scientific
execution is disabled. Native CPU50 retains its existing617-byte identity,
SHA256 `3288e0ef61cf3e628dcd827f9cf003c9d6ec6b5a12169e7a8bfc796baacddba7`.

Reuse the read-only qualified-archive provenance parser, preserving historical
source/README snapshots; never invoke an old controller or stale verifier.
Freeze all actual source/compiler/dependency/link/runtime inputs before use.
Repeated independent source reviews and committed/pushed source precede the
sole launch; exact and independent full retained audits precede result updates.

Even PASS qualifies only these prototype static-call lifecycles on this host.
It is not installed speed, new recovery evidence, shared-call/cold-start/
non-GFNI-host speed, an all-K guarantee, or proof of restored preserved paths.

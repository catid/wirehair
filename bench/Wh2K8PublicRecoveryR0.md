# Installed WHV2 K8 retained recovery R0

Protocol `wirehair.wh2.k8-public-recovery-r0`; sole scientific namespace
`/var/tmp/wh2-k8-public-recovery-r0`. Prospective freeze is in
`wirehair-sxvz.16.1.20.82.5.3.2`, before implementation or codec workloads.

Compare actual explicit WHV2 K8 (`0x7a9276b85c730ae0`), ordinary WH2 (must
select certified `0x4b295bbb47f4f9c9`), and WH1 under both ownership policies.
The candidate is not a benchmark facade. Report its actual WHV2 descriptor;
do not translate it into WHK8 or change ordinary K8 selection.

Preserve exactly 6,260 records and 74,946 packet IDs from sealed K8 evidence:
6,144 retained loss traces, 72 hard traces and 44 historical origins. Widths
are 2/64/1280, message bytes are `7*B+tail`, ID7 emits only its tail, and all
original history widths/tails/horizons remain unchanged. No new roots, loss
samples, equation selection, overhead extension or timing occurs. The 512
roots are shared across twelve cells; backend/policy repetitions do not
increase the statistical sample size.

Each real encoder and eight source-basis encoders are destroyed before any
receiver. Destroy independent inputs before encoding and check borrowed input
immutability. Derive candidate rows independently from the sealed lambda-2
pair. Reconstruct every arm's real packet with polynomial arithmetic, check
every prefix against independent rank, recover twice into guarded buffers,
and retain attempted API ledgers. Repeated descriptor/ID equations and payloads
must agree. Compare all six arms with authenticated old prototype records:
only explicitly checked candidate descriptors may differ; neither record
stream is rewritten. This does not equate the APIs' error/detach/conflict rules.

PASS requires all three independently reported predicates:

1. Complete execution, provenance, oracle and backend agreement.
2. Each candidate policy has at most 1% OH0 failures overall and in every
   512-trace cell (at most five); all 72 hard cases recover at OH0 and all 44
   histories recover within their original horizons.
3. Fixes exceed introductions against each ownership-matched control at OH0.

A valid nonwinner is FAIL, not INVALID or unconditional PASS. Report paired
fixes/introductions at OH0..4, omitting historical horizons not originally
available. Aggregate superiority is not per-trace or population dominance.

Before science, qualify 48 bounded neutral cases, twelve late-call cleanup
injections, descriptor rejection, all-arm field mutations, consistently forged
repeated control equations, receipt omissions/backend rebinding, nonwinner and
integer threshold boundaries, output caps, incomplete publication and spent
namespace refusal. Preserve the separate installed API correctness evidence;
this run does not exercise seam subsets or allocation/detach/conflict contracts.

Use exact current production archives, reproduced from all nineteen original
CMake compile recipes with complete pre/post dependency checks, member-byte
comparison and single-runtime symbol/ABI checks. Build the new observer only
after its producing inputs are pinned. No `wh2_small_*` implementation is
linked. Native, portable and ASan/UBSan must agree. Freeze all actual compiler,
header, linker/runtime, Python and retained-data dependencies; commit/push
source and complete repeated source reviews before the sole scientific launch.

Worker limits: CPU180s/wall210s/address space384MiB (ASan shadow exempt).
Observer245s/backend; total900s including verification. Raw96MiB/backend,
stderr64KiB/backend, sealed bundle384MiB. Keep every record and failure prefix.
Never rerun, filter, resize, reselect, rescore or rescue a spent experiment.
Exact and independently written full audits precede outcome publication and
source advancement. No speed, default promotion, all-K or all-seed claim.

## Audited installed result

The sole run at source `5a300505da3754dbdbb8cbfcba4b8a79bad1b0cb`
completed **PASS**. Execution validity, the frozen recovery target and paired
aggregate superiority pass under both source policies through the actual
installed WHV2 API. Native, portable and ASan/UBSan agree on all 6,260 records,
74,946 packet IDs and six arms. All workers completed successfully with empty
stderr; the complete controller took 66.611779 seconds.

Failures on the 6,144 retained loss traces:

| Codec, either matched source policy | OH0 failures | Observed rate | OH1 | OH2 | OH3 | OH4 |
|---|---:|---:|---:|---:|---:|---:|
| Installed explicit K8 | 12 | 0.1953125% | 0 | 0 | 0 | 0 |
| WH1 | 37 | 0.6022% | 5 | 3 | 2 | 0 |
| Ordinary WH2 | 246 | 4.0039% | 3 | 0 | 0 | 0 |

K8 has approximately 67.6% fewer aggregate zero-overhead failures than WH1
and 95.1% fewer than ordinary WH2. Against WH1 it fixes 37 failures and
introduces 12; against ordinary WH2 it fixes 245 and introduces 11, with one
shared failure. This is not per-trace or per-cell dominance: the two-byte
burst cell has two candidate failures versus ordinary WH2's one. Every
candidate cell meets the frozen 1% threshold; the worst is 3/512 (0.5859375%).
All twelve candidate failures recover with one additional packet.

All 72 separate hard cases recover at zero overhead for K8 and WH1; ordinary
WH2 has six failures at zero overhead and one after one extra packet. All 44
historical origins recover within their original, unextended horizons for K8,
versus five unresolved WH1 and 41 unresolved ordinary-WH2 origins. These cases
are outside the 6,144 denominator. The twelve cells share 512 roots; policy
and backend repetitions are not additional independent recovery samples.

Every candidate coefficient, every-arm polynomial packet hash, prefix rank,
first success, repeated guarded recovery and attempted-call ledger passes.
The installed records reproduce all six authenticated prototype arms with
only the separately validated candidate descriptor difference. Neither stream
is rewritten. This confirms retained recovery behavior, not equality of the
APIs' different ownership/detach/conflict contracts.

Before execution, all nineteen production objects and the complete original
archive were reproduced byte-for-byte in each backend. Forty-eight neutral
cases and twelve late-call cleanup checks passed per backend, plus descriptor
rejection and claim/CLI checks. All 27 controller/build tests passed under
Python 3.12 and 3.8. An initial prelaunch link-closure check correctly rejected
the omitted sanitizer startup object `libasan_preinit.o`; the dependency list
and regression test were fixed and all three builds requalified in a fresh
directory. That preserved build failure was not a scientific run.

Exact retained replays pass on Python 3.12 and 3.8 with the frozen sanitizer
environment. A separately written standard-library auditor independently
derives coefficients, payloads, normalized ranks, every group/cell and paired
failure list; it parses archive/ELF data and verifies the full 1,287-pin build
closure and nonmutating prototype comparison. Both complete audits pass and
their reports match except elapsed time. All sixteen auditor selftests pass
on both interpreters. Repeated main and independent source-reading passes
are clean; audits finished before HEAD or pinned documentation advanced.

Retained evidence:

- Bundle: `/var/tmp/wh2-k8-public-recovery-r0`, 136,315,977 bytes.
- COMPLETE SHA256: `699415eef17fcc486aeb3e5487feef0a9dcd4812110497df1370014c47b7af02`.
- Native raw SHA256: `bab96ca60d697f9f5a751fdbc74dce306c6c0d12a98e651f7e9735beb6af50e1`.
- CLAIM SHA256: `b5335bb0aa2d0e8f354aebc2df5b3d8d51f65f7070e45b6e22b44499c5819ab1`.
- Qualification: `/tmp/wh2-k8-public-recovery-qualified.i7v7bPnq`.
- Independent audits: `/tmp/wh2-k8-public-recovery-independent-audit.9s3KOUs5`.
- Auditor SHA256: `2867f8ea2b95d485adf77b0b1cd9211e0bef11edf93c0d087c1aed65f13f6c23`.

This namespace is permanently spent. Installed K8 speed, preserved-path
restoration and current K3 speed retention remain unqualified. Defaults are
unchanged; this is not a new holdout, population bound, all-K guarantee or
performance promotion.

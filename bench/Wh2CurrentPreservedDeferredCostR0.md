# Current installed preserved-path deferred cost R0

Frozen before implementation in `wirehair-sxvz.16.1.20.84.3.1`.
Protocol: `wirehair.wh2.current-preserved-deferred-cost-r0`.
Sole namespace: `/var/tmp/wh2-current-preserved-deferred-cost-r0`.

Compare the exact pre-admission shared library (`4eb4f53c...`) against the
current K5-integrated native-default shared library (`0a3b1b96...`). These are
the original, unmodified files, not reconstructed or rewritten timing copies.
Both fresh-process load orders are mandatory. Historical producer chains and
the current 18-object archive/dependency/recipe chain are authenticated; both
DSOs must relink byte-identically into separate, never-loaded proof files.
The previously lost historical CTest diagnostic remains explicitly disclosed.

The existing 20-case preserved-path gate is unchanged: certified borrowed
K2/3/4/6/128 at B2/1280; certified independent K3 at both widths; opt-in small
K3 at both widths and source policies; WH1 borrowed K3 and opt-in borrowed K6
at both widths. Each encoder emits K systematic, eight low-ID repairs and six
distant repairs. Decoders receive low eight, distant six, then systematic
packets through their own first success. Every lifecycle includes creation and
free, with exact output and ledger checks.

Keep batch 128 (K128 uses four), 51,840 callbacks per load order, 12 replicates,
both measurement orders and all 48 completion-relative delay phases. CPU50 and
its exact binary identity are unchanged. All 160 same-code controls per load
order require 95% log intervals strictly inside +/-log(1.02). A resolved
slowdown (lower log bound >0) is a regression even below 2%; upper log bound
>=log(1.02) is uncertainty. Failed controls dominate. Never filter, pool,
resize, rerun or rescore a spent cohort.

The new adapter includes the original worker to reuse its exact WORK, clocks,
fixture construction, output validation and roster. It changes publication:
header, retained records and footer are emitted only after measurement ends.
The header uses immutable reference fixtures, including on late source
corruption. All initial/warm/failing records remain retained. No result-output
thread, queue, extra per-callback allocation or subtraction is introduced.
The original worker/configuration defaults remain unchanged.

Neutral qualification includes the existing 160 WORK and 32 failure cases per
load order plus three-record deferred streams for success, final Recover
failure/exception, final clock failure and late source mutation. Early output
is rejected; every sink-failure position is terminal with no retry. Actual
full-device and broken-pipe errors must be reported. Repeat under an
instrumented driver with ASAN/leak/fake-stack and UBSAN; the original native
DSOs are not sanitizer-instrumented. Python 3.8/3.12 reader tests and repeated
source-reading reviews precede the sole scientific launch.

Worker limits: CPU180s, wall210s, WORK150s, address space512MiB. Observer240s
per worker, controller600s. Raw192MiB per load order, stderr64KiB, bundle448MiB.
No allocator/loader overrides. Commit and push qualified source before receipt
and launch; exact and independently written whole raw/statistical/provenance
audits precede any report or HEAD advance.

This screen does not qualify ordinary K3 speed retention, strict WH1 speed,
static-library speed, recovery rate, defaults or all-K performance. Original
admission REGRESSION, isolation CONTROL_FAIL and installed K5 CONTROL_FAIL
remain historical outcomes, not results that this screen can relabel.

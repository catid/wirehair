# Actual ordinary K8 lifecycle gate R0

Prospectively frozen before implementation in
wirehair-sxvz.16.1.20.82.5.3.4.1. Protocol
wirehair.wh2.k8-ordinary-cost-r0; sole future scientific namespace
/var/tmp/wh2-k8-ordinary-cost-r0. No timing has been launched.

This instrument derives fresh external source files from five complete
SHA-pinned historical K8 public-cost templates. Old controllers and builders
are input text, never imported, patched or run. A new derivation receipt binds
every generated file and the generator. Historical scientific namespaces are
unchanged. The preparation entry point exposes only prepare, verify and build;
it does not forward scientific run/receipt/replay commands.

The only timed API-route changes are:

| Arms | Route |
| --- | --- |
| 0 / 3 | Explicit certified WH2, independent / borrowed |
| 1 / 4 | Candidate ordinary WH2, independent bare constructor / borrowed options |
| 2 / 5 | Unchanged actual WH1 owned / borrowed |

The exact six-line selector, source SHA-256
172324fd048daf154267f60a6e76df1e1b9a2545b9eac49059d0be21cfe99a3f,
is reproduced from the qualified native/scalar/ASan compile recipes and linked
as an explicit object before the original library. All nineteen original
objects and the complete archive must first reproduce byte-exactly. The new
proof also binds the selector's source, original and reproduced objects,
recipe, compiler dependencies and forty-test qualification log. Link maps
must show that the old Profile archive member was not extracted. There is
one actual API implementation and matching GF backend.

## Frozen workload and decision

Keep the original WORK body, six shapes, roster, clocks, statistical decisions,
deferred output and failure retention unchanged. Six shapes are block widths
2/64/1280 with full and one-byte final blocks, message length 7*B+tail.
Encoder time includes create, descriptor, 24 packets and free: IDs0..15 plus
UINT32_MAX-2*j, j0..7. Each low/distant decoder stream supplies eight repairs
then eight systematics. Charge create, every feed through that arm's own first
success, recover and free, retaining any systematic fallback.

Batch128, twelve replicates, both measurement orders, eighteen positions,
77,760 callbacks, all 48 relative-delay phases. All 216 same-code 95% log
intervals must lie strictly inside +/-log(1.02); all 144 candidate upper95
time ratios must be below one against both matched controls. No 5% minimum,
candidate regression tolerance, order pooling, filtering, subtraction, resized
cohort, rerun, retuning or subset rescue. A failed control is CONTROL_FAIL;
a valid nonwinner is FAIL; oracle/identity/infrastructure failure is INVALID.

Native CPU50 and its existing 617-byte identity; WORK180s, CPU240s, wall300s,
address space384MiB, observer330s/controller480s, raw192MiB, stderr64KiB and
bundle256MiB. Portable and fully instrumented sanitizer builds are neutral
only. Preserve all 216 actual neutral WORK cases and full fixture arithmetic,
rank, ownership, partial-tail, error/exception, descriptor-rejection,
authentication and publication-failure checks before measurement.

Commit/push qualified sources, pin the actual native launch, and independently
audit full raw data and producing inputs before advancing source or reports.
This static six-arm gate cannot qualify preserved-path restoration, current
K3 retention, shared-call/cold-start/non-GFNI-host/all-K speed or a new recovery
rate. Those admission requirements remain separate. Production defaults are
unchanged.
